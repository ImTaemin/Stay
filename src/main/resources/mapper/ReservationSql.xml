<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="stay.data.mapper.ReservationMapper">
	<insert id="insertReservation" parameterType="redto">
		insert into reservation values (null, #{guest_id}, #{host_id}, #{start_date}, #{end_date}, #{price}, #{pay_method},
		<if test="card_num != null">
			#{card_num}
		</if>
		, null, now());
	</insert>
</mapper>